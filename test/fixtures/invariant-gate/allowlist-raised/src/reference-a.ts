/* Deliberately retained reference code: nothing on the product path imports it,
 * and that is the point. Stands in for the modules an architectural change
 * retires from the export graph without deleting. */
export const referenceA = (): number => 1
